# SmartDaaS — platform dependencies
# These are the dependencies the deployed Streamlit application actually needs.
# Install with: pip install -r requirements-platform.txt
#
# The previous single requirements.txt listed v2 research dependencies
# (torch, econml, scikit-survival) and omitted streamlit, shap, fpdf2,
# supabase, requests and openpyxl — so it could not build the platform.
# The Dockerfile was what actually deployed. Research dependencies now live
# in requirements-research.txt.

# Core
streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Model
scikit-learn>=1.3.0
shap>=0.44.0

# Reads the source .xlsx
openpyxl>=3.1.0

# Visualisation
matplotlib>=3.7.0

# PDF export (reports.py)
fpdf2>=2.7.0

# Audit logging (optional at runtime — the app degrades gracefully without it)
supabase>=2.0.0

# DHIS2 connector
requests>=2.31.0

# Optional: LLM rewording layer in the outreach brief.
# Only used when ANTHROPIC_API_KEY is set. If set, outreach brief text is
# sent to a third-party API. Leave unset and uninstalled to disable.
# anthropic>=0.25.0

# Optional: scoring API (api.py). Not needed for the Streamlit app.
# fastapi>=0.110.0
# uvicorn>=0.27.0
