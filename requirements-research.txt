# SmartDaaS — v2 research / prototype dependencies
# These are NOT needed to run the platform. They support the v2 architecture
# prototypes (survival model, transformer encoder, multi-task learning,
# causal/uplift modelling), all of which are synthetic-data prototypes.
# See LIMITATIONS.md §1.
#
# For the deployed application, use requirements.txt.

# Core data
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Machine learning
scikit-learn>=1.3.0
scikit-survival>=0.21.0

# Deep learning (transformer encoder, multi-task)
torch>=2.0.0

# Causal / uplift modelling
econml>=0.14.0

# Visualisation
matplotlib>=3.7.0

# Optional: experiment tracking
# mlflow>=2.0.0
# wandb>=0.15.0
